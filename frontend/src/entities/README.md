# Entities

Tek bir iş nesnesi etrafında dönen **küçük** UI ve veri parçaları (ör. `CustomerCard`, `OrderStatusBadge`).

Feature’lar arası tekrar bu katmanda toplanır; iş kuralı yok, sadece sunum + hafif formatlama.
