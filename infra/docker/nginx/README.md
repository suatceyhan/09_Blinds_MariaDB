# Nginx şablonları

- Ön uç statik dosya sunumu
- `api` upstream proxy (`proxy_pass` → backend konteyner veya host port)
- TLS genelde Plesk veya üst katman load balancer’da sonlandırılır; burada iç ağ örnekleri olabilir.
