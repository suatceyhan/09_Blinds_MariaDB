# Backend testleri

- **`unit/`:** servis ve pure fonksiyon; DB mock.
- **`integration/`:** gerçek veya testcontainer DB ile repository API.
- **`contract/`:** OpenAPI şemasına uygunluk (ör. schemathesis).

`conftest.py` kökte paylaşılan fixture’lar için kullanılır.
