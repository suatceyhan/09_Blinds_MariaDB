# Core

- Uygulama ayarları (`pydantic-settings` / `config`).
- JWT, şifre hash, güvenlik yardımcıları.
- Yapılandırılmış log / correlation id desteği için temel fonksiyonlar.
- Ortak FastAPI `Depends` (ör. `get_db`, `get_current_user`).
