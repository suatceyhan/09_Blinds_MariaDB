# Veritabanı oturumu

- SQLAlchemy `engine`, `SessionLocal`, `get_db` generator.
- `Base` ve model import sırası (Alembic ile uyumlu).

PostgreSQL bağlantısı ortam değişkeni üzerinden (`DATABASE_URL`).
