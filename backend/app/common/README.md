# Common (çapraz kesit)

- Uygulama genelinde exception sınıfları ve hata eşleme (HTTP status).
- Sayfalama, sıralama DTO’ları; ortak enum’lar.
- **Dikkat:** domain iş kuralı buraya taşınmamalı; çok şişerse `core` ile sınır gözden geçirilir.
