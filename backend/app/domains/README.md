# Domains (iş modülleri)

Her alt klasör bir **bounded context** benzeri pakettir: kendi model, repo, servis ve isteğe bağlı router.

Yeni iş alanı eklendiğinde burada yeni klasör açılır; `api/v1/router` içine bağlanır.
